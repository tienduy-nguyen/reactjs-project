{
  "name": "react-testing",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build:client": "yarn --cwd client build",
    "dev:client": "yarn --cwd client start",
    "dev:server": "yarn --cwd server start",
    "start:dev": "concurrently \"yarn dev:client\" \"yarn dev:server\"",
    "test:client": "yarn --cwd client test"
  },
  "dependencies": {
    "concurrently": "^5.3.0"
  }
}